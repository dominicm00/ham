paths =
	foo
	bar.a
	foobar.bee
	path/foo
	path/foo.a
	some/path/bar.bee
	/an/absolute/path/foobar.so
	/
	<grist>some/path/bar.bee
	foo(archive.o)
	bar.a(archive.o)
	foobar.bee(archive.o)
	path/foo(archive.o)
	path/foo.a(archive.o)
	some/path/bar.bee(archive.o)
	/an/absolute/path/foobar.so(archive.o)
	/(archive.o)
	<grist>some/path/bar.bee(archive.o)
;
for result in "$(paths:%1)" {
	Echo $(result) ;
}
---
M=
-
foo
bar.a
foobar.bee
path/foo
path/foo.a
some/path/bar.bee
/an/absolute/path/foobar.so
/
<grist>some/path/bar.bee
foo
bar.a
foobar.bee
path/foo
path/foo.a
some/path/bar.bee
/an/absolute/path/foobar.so
/
<grist>some/path/bar.bee
---
M=member
-
foo(member)
bar.a(member)
foobar.bee(member)
path/foo(member)
path/foo.a(member)
some/path/bar.bee(member)
/an/absolute/path/foobar.so(member)
/(member)
<grist>some/path/bar.bee(member)
foo(member)
bar.a(member)
foobar.bee(member)
path/foo(member)
path/foo.a(member)
some/path/bar.bee(member)
/an/absolute/path/foobar.so(member)
/(member)
<grist>some/path/bar.bee(member)
---
M=member.suffix
-
foo(member.suffix)
bar.a(member.suffix)
foobar.bee(member.suffix)
path/foo(member.suffix)
path/foo.a(member.suffix)
some/path/bar.bee(member.suffix)
/an/absolute/path/foobar.so(member.suffix)
/(member.suffix)
<grist>some/path/bar.bee(member.suffix)
foo(member.suffix)
bar.a(member.suffix)
foobar.bee(member.suffix)
path/foo(member.suffix)
path/foo.a(member.suffix)
some/path/bar.bee(member.suffix)
/an/absolute/path/foobar.so(member.suffix)
/(member.suffix)
<grist>some/path/bar.bee(member.suffix)
---
#M=(member)
#-
#foo((member))
#bar.a((member))
#foobar.bee((member))
#path/foo((member))
#path/foo.a((member))
#some/path/bar.bee((member))
#/an/absolute/path/foobar.so((member))
#/((member))
#<grist>some/path/bar.bee((member))
#foo((member))
#bar.a((member))
#foobar.bee((member))
#path/foo((member))
#path/foo.a((member))
#some/path/bar.bee((member))
#/an/absolute/path/foobar.so((member))
#/((member))
#<grist>some/path/bar.bee((member))
#---
